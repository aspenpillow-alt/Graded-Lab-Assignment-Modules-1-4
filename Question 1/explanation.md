1,I used whoami and groups to verify my user identity and group memberships on the system.
2,I used pwd to find my location and ls -l to see all files in the long-listing format.
3,I created user_info.txt using the echo command to confirm my environment is ready.
4,I used wc -c to check the file size in characters to ensure it was saved correctly.
5,"I checked the mkdir manual and found the -p option, which helps create parent directories."
6,I listed my home directory and used sort to organize the output alphabetically.
7,"I used grep to filter the log.txt file specifically for lines containing the word ""admin."""
8,I used uname -r to display the specific version of the Linux kernel currently running.
9,I sent 4 ICMP packets to Google using ping to verify my network connection.
10,I used uptime to check how long the system has been running and the current system load.

